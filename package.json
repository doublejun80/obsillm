{
  "name": "obsillm",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin for grounded note generation with OpenAI and Gemini.",
  "scripts": {
    "build": "npm run typecheck && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.10",
    "typescript": "^5.9.0",
    "vitest": "^3.1.0"
  }
}
